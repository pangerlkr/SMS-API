interface:
  display_name: "SMS API"
  short_description: "Repo-specific patterns and workflows for SMS-API"
  default_prompt: "Use the SMS-API repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true